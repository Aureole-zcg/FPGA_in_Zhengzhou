以太网帧尾的 **FCS（帧校验序列）** 校验，其核心机制是 **CRC-32（循环冗余校验）** 算法。这是一个强大的错误检测码，用于确保接收到的数据在传输过程中没有被损坏。

整个过程可以分为发送端计算插入和接收端验证两个阶段。

### 📤 发送端：计算并插入FCS

在发送数据帧前，发送端网卡会执行以下步骤：

1.  **划定计算范围**：FCS的计算范围不包括前导码、帧起始定界符（SFD）和FCS字段本身。它涵盖从**目的MAC地址**开始，一直到**数据负载（Payload）** 结束的所有字节。如果数据负载不足46字节，需要先填充（Pad）到最小长度，填充字节也参与计算。

2.  **执行CRC-32计算**：网卡使用IEEE 802.3标准定义的CRC-32算法对上述范围内的所有字节进行计算。该算法基于一个特定的**生成多项式（Generator Polynomial）**：
    `G(x) = x³² + x²⁶ + x²³ + x²² + x¹⁶ + x¹² + x¹¹ + x¹⁰ + x⁸ + x⁷ + x⁵ + x⁴ + x² + x + 1`

    这个多项式的核心思想是，将待发送的数据看作一个巨大的二进制数（多项式 `M(x)`），然后用它除以这个生成多项式 `G(x)`。这个除法得到的**32位余数**，就是原始的CRC校验值。

3.  **处理CRC结果并放置到帧尾**：计算出的32位原始CRC值不能直接使用，需要经过以下转换：
    *   **位序反转（Bit-reversed）**：将32位CRC值的位顺序完全颠倒。
    *   **按位取反（Bitwise NOT）**：将反转后的32位数值逐位取反（0变1，1变0）。
    经过这两步处理后的32位数值，才是最终填入帧尾FCS字段的值。

### 📥 接收端：验证FCS

当接收端网卡收到一个完整的以太网帧后，会进行如下验证：

1.  **同样范围再计算**：接收端网卡对收到的帧，从**目的MAC地址**到**数据负载**（同样排除前导码、SFD和FCS字段）这部分数据，使用**完全相同**的CRC-32算法再计算一遍。

2.  **比较结果**：接收端会将这个**重新计算**出的32位数值，与帧尾附带的**FCS字段**里的值进行比较。

3.  **判定帧的有效性**：
    *   **一致（通过）**：如果两个值完全一致，则认为帧在传输过程中没有发生错误，接收端会将其向上层协议传递。
    *   **不一致（丢弃）**：如果两个值不同，则判定帧已损坏。接收端会**直接丢弃该帧**，且不会通知发送端重传。以太网本身只负责检错，不负责纠错或重传，重传机制由更高层的协议（如TCP）来保证。

> **补充：一种更高效的“魔术数字”验证法**
> 在实际硬件实现中，接收端有时会采用更高效的方法：它将包括**原始FCS字段**在内的整个帧数据一起送入CRC计算器。对于一个正确的帧，最终计算器会输出一个固定的32位常数 **`0xC704DD7B`**。如果输出是这个数，就代表校验通过；否则，帧有错误。这种方法免去了额外的比较步骤。

### 💎 关键细节与注意事项

*   **CRC初始值**：CRC-32计算器的初始值（初值）不是0，而是 **`0xFFFFFFFF`**。
*   **最终异或**：除了位序反转，最终结果还需要与 **`0xFFFFFFFF`** 进行异或（即按位取反）。
*   **FCS的发送顺序**：FCS字段在发送时，是**最高有效位（MSB）先发**，这与帧内其他字段（如MAC地址）的**最低有效位（LSB）先发**规则不同。

简单来说，FCS的校验过程就是：**发送方算一个数附在帧尾，接收方用同样方法再算一遍，两个数能对上就收，对不上就扔。**
