以下是FIR与IIR数字滤波器的详细对比表格，从信号处理理论到FPGA硬件工程实现均有覆盖：

| 对比维度 | **FIR（有限脉冲响应）** | **IIR（无限脉冲响应）** |
| :--- | :--- | :--- |
| **1. 实现结构** | **非递归**（前向馈送）。输出仅由`输入 * 系数`累加得到，无数据反馈回路。 | **递归**（反馈）。输出由`输入 * 系数` + `历史输出 * 系数`共同决定。 |
| **2. 单位脉冲响应** | **有限长度**。输入一个脉冲，输出在有限个时钟周期后**彻底归零**。 | **无限长度**。输入一个脉冲，输出理论上**永不休止**（指数衰减趋近于零）。 |
| **3. 相位特性** | **严格的线性相位**（若系数对称）。信号波形保真度极高，无相位失真。 | **非线性相位**（群延迟变化）。若对相位敏感，需额外增加全通滤波器校正。 |
| **4. 频率截止特性** | 实现陡峭截止需**极高阶数**（如几百~几千阶）。过渡带较宽时效率低。 | 极低阶数（如4~8阶）即可实现**极陡峭**的截止特性。 |
| **5. DSP资源消耗** | **极高**。消耗大量DSP Slice（乘法器）和BRAM（存储系数/数据）。 | **极低**。消耗极少的DSP资源和BRAM。 |
| **6. 稳定性** | **绝对稳定**。无反馈回路，系统极点全部位于原点（Z平面），任何系数不会引起振荡。 | **存在风险**。极点位置随系数变化，温度漂移或定点数截断可能导致极点移出单位圆，引发自激振荡。 |
| **7. 硬件时序收敛** | **易于收敛**。结构为纯前向链，可插入大量流水线（Pipeline）寄存器，轻松跑高频（如>250MHz）。 | **时序瓶颈**。反馈回路（递归环）无法插入流水线，导致最大时钟频率（Fmax）严重受限（通常<100~150MHz）。 |
| **8. ADC采样率匹配** | 适合**高采样率**（如>100MSPS）的过采样系统，可结合抽取（CIC/半带）提升效率。 | 适合**低采样率**（如<10MSPS）或窄带信号，反馈延迟限制了其在高速流水线中的吞吐率。 |
| **9. 有限字长效应** | **较弱**。系数量化误差影响较小，不易产生极限环振荡。 | **较强**。系数量化、乘法舍入误差极易导致零极点偏移，且容易产生小信号极限环（Dead-band效应）。 |
| **10. 系数计算难度** | **简单直观**。通常采用窗函数法或频率采样法，MATLAB (fir1) 一键生成。 | **较复杂**。需借助Butterworth/Chebyshev模拟原型转换，需精确控制极点位置。 |
| **11. 典型应用场景** | - 雷达/通信基带（必须保相位）<br>- 高速ADC抗混叠抽取滤波<br>- 匹配滤波（如扩频码同步） | - 音频降噪/均衡器<br>- 工频（50Hz）陷波<br>- 低频传感器数据平滑（温度/压力） |
| **12. 频率响应类型** | 严格保证线性相位，但幅度响应（幅频特性）在相同阶数下不如IIR理想。 | 幅度响应（幅频特性）极佳，但相位响应较差（除非使用Bessel近似）。 |

### 工程选型建议
-   **选FIR**：当**FPGA资源足够**且信号对**相位失真零容忍**时（99%的高速采集/通信场合）。
-   **选IIR**：当**FPGA资源极为紧张**、**时钟频率较低**且对**相位完全不关心**（如仅做直流滤波）时。
