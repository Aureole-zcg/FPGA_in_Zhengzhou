IDELAY2的参考时钟一般都是200Mhz（70%），有时候也用300Mhz  
IDELAY2的4种工作模式：固定延迟模式（fixed），可变延迟模式，可变加载延迟模式，可变加载流水模式  
IDELAY2的输入信号是数据还是时钟信号要声明


电路板的等长线严格控制在25密尔的误差（100mil=2.54mm）

研究所发布的《动态相位调整技术DPA》：芯片内部动态地调整Idelay原语：训练模式累加的，模拟信号是锯齿波，出先毛刺的是错误处（正确接收ADC的测试数据应该是无毛刺光滑的）


SPI的4个引脚给ADC配置数据，配置0F模式为测试模式，发现是毛刺的锯齿波，证明不正常，正常后，拿信号源用ADC接收出来

延迟按挡位（tap）划分共32挡位（0~31）整数档，参考时钟200MHz  
参考时钟200MHz时钟，每一档（tap）延迟=T/2 /32 * 1000 = 78.125ps（0档延时600ps，1档延时678ps，2档延时756ps）（每个档位对应的延迟是某个基本单位的一半，所以先除以2）
别搞混：（SDRAM的刷新时间：一行7.8125us）  
调用多个IDELAY2需要调用1个IDELAYCTRL原语  

面试题：ADC的采样频率是250MHz，分辨率位宽是16bit，ADC和fpga之间用LVDS传输。问：每一条传输信号线的传输速率多快？  250*16/8（因为是8条差分信号线）

ADS42LB69 锯齿波毛刺情形
情形1：延迟时钟：1.延迟时钟   2.锁相环移相  但是延迟多少得测试：{1步：配置寄存器0F将ADC输出成测试/训练模式的累加模式，2步：先延时相位，抓波形发现不是完美的累加模式，证明是错的，直到相位对为止, 因此工作时，会产生大量的测试工作，测两个月的（可以找一直找中间值最快找到正确数据位置，保证长时间数据正确才算完工）

情形2：延迟数据：一样test模式的递增累加模式，延迟数据之后找到正确位置

情形3：展开位宽时，发现抓波形只有个别bit位错了，对该bit位进行设置即可(原本锯齿波，波形却有毛刺，此时观测数据位即可找到单独bit位)

情形4：电路板重大问题(很少出现)，哪个bit位都得调，乱套了，配合其他工程师，检查电路板是否等长




